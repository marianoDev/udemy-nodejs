const nombre = 'Deadpool';
const real = 'Wade Willson';

const normal = nombre + ' ' + real;
const templates = `${nombre} ${real}`;

console.log( normal);
console.log(templates);

const html = `

<h1> Hola </h1>
<p> Mundo </p>

`;